# Bounded Investigation Engine

Pure request/evidence preparation and strict response parsing. No filesystem,
network, keychain or database access. Evidence is restricted to captured scan
chunks supplied by the native backend. See the investigation contract.
